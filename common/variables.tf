variable "aws_profile" {}
variable "aws_region" {}

# Add the remainder of your global variables here
# Each variable defined here should be instantiated in either

# environment/terraform.tfvars
# environment/$ENV/$ENV.tfvars   (each environment's tfvars, such as staging.tfvars)
